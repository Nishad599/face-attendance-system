# Database Configuration
# Leave blank or set sqlite:///attendance.db for SQLite (default local development)
# Set to a postgresql URL for production PostgreSQL deployment:
# DATABASE_URL=postgresql://username:password@localhost:5432/attendance_db
DATABASE_URL=
